const firstName = "Sara";//1. using number as variable name is not allowed
let myAge = 22;//2. hyphen is not allowed in variable name
let score = 0;
score = 100; //3. changed const to let
let bad = "bad";//4. reusing let as variable name

console.log(firstName);
console.log(myAge);
console.log(score);
console.log(bad);